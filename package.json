{
  "name": "racketapp-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "dev:backend": "pnpm --filter backend dev"
  }
}
